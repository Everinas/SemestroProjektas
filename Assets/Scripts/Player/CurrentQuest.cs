using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class CurrentQuest : MonoBehaviour
{

    public bool friendlyChat;
    public bool lookingForApples;
    public bool arqosMagic; 

    public bool shoeHunt;
    public bool findThePortal;
    public bool iceCreamFlavor;
    public bool theFinale;

    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
